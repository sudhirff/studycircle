export default {
    coursesTypes(state) {
        return state.coursesTypes;
    },

    coursesType(state) {
        return state.coursesType;
    },

    currentPage(state) {
        return state.currentPage;
    },

    resources(state) {
        return state.resources;
    }
}