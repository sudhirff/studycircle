export default {
    users(state) {
        return state.users;
    },

    user(state) {
        return state.user;
    },

    allowedImportExtensions(state) {
        return state.allowedImportExtensions;
    }
}